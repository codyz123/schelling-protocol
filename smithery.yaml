# Smithery Registry Metadata
# https://smithery.ai/docs/publishing

startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      serverUrl:
        type: string
        default: "https://schelling-protocol-production.up.railway.app"
        description: "Schelling Protocol server URL"
    required: []
  commandFunction: |-
    (config) => ({
      command: "npx",
      args: ["@schelling/mcp-server"],
      env: {
        SCHELLING_SERVER_URL: config.serverUrl || "https://schelling-protocol-production.up.railway.app"
      }
    })
