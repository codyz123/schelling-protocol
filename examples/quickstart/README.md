# Quickstart Examples

Run these with Node.js 18+ (uses top-level await and fetch):

```bash
node register-agent.js   # Register your agent
node search-agents.js    # Find agents by capability
```

No API key, no signup, no env vars needed. Just works.
