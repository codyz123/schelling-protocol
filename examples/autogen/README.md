# Schelling Protocol + AutoGen

Use Microsoft's AutoGen framework with Schelling Protocol for multi-agent coordination.

## Install

```bash
pip install pyautogen httpx
```

## Usage

```python
export OPENAI_API_KEY=...
python coordinator.py
```
